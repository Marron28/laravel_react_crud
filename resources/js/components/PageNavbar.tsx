import NavbarContent from './NavbarContent';

export default function PageNavbar() {
    return (
        <nav className="nav-shell sticky top-0 z-50 bg-white shadow-md">
            <NavbarContent variant="black" />
        </nav>
    );
}
